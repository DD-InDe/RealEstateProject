<Page x:Class="RealtorSystemDesk.Pages.ClientManagePages.ClientInfoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:RealtorSystemDesk.Pages.ClientManagePages"
      mc:Ignorable="d"
      Loaded="ClientInfoPage_OnLoaded"
      Title="Информация о клиенте"
      d:DesignHeight="450"
      d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0"
                    Orientation="Vertical">
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Информация о клиенте" />

            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Фамилия:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding LastName}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Имя:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding FirstName}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Отчество:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding MiddleName}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Дата рождения:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding BirthDate}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Телефон:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding Phone}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Почта:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding Email}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Пол:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding Gender.Name}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Серия паспорта:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding Passport.Serial}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Номер паспорта:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding Passport.Number}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Дата выдачи паспорта:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding Passport.IssuedDate}" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Кем выдан паспорт:" />
            <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="{Binding Passport.IssuedBy}" />

            <Button x:Name="EditButton"
                    Content="Редактировать данные"
                    Click="EditButton_OnClick" />
            <Button x:Name="ArchiveButton"
                    Content="Архивировать"
                    Click="ArchiveClientButton_OnClick" />
        </StackPanel>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Контракты" />
                <Button x:Name="AddContractButton"
                        Content="Добавить контракт"
                        Click="AddContractButton_OnClick" />
                <DataGrid x:Name="ContractDataGrid"
                          IsReadOnly="True"
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="*"
                                            Header="Дата составления:"
                                            Binding="{Binding DateCreate}" />
                        <DataGridTextColumn Width="*"
                                            Header="Действителен до:"
                                            Binding="{Binding ValidUntil}" />
                        <DataGridTextColumn Width="*"
                                            Header="Тип:"
                                            Binding="{Binding Type.Name}" />
                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Объект"
                                            Visibility="{Binding ObjectButtonVisibility}"
                                            Click="NavigateToObject_OnClick" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Печать"
                                            Click="PrintButton_OnClick" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>

            <StackPanel Orientation="Vertical"
                        Grid.Row="1">
                <TextBlock
                               Margin="7"
                               Style="{DynamicResource MaterialDesignBodyMediumTextBlock}"
                               VerticalAlignment="Center" Text="Документы" />
                <Button  x:Name="AddDocumentButton" Content="Загрузить"
                         Click="DocumentAddButton_OnClick" />
                <DataGrid x:Name="FileDataGrid"
                          IsReadOnly="True"
                          AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="*"
                                            Header="Название файла:"
                                            Binding="{Binding File.FileName}" />
                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Скачать и открыть"
                                            Click="DocumentLoadButton_OnClick" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Удалить"
                                            Click="DocumentDeleteButton_OnClick" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>